name: Svetlana Model Contract
on:
  push:
  pull_request:
jobs:
  model-contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node qa/model-morph-contract.test.js
